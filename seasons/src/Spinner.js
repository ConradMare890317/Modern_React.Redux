import React from 'react';

const Spinner = () => {
    return (
        <div className="ui active dimmer">
            <div className="ui massive text loader">Choose one, Dumb.Dumb!!...</div>
        </div>
    );
};

export default Spinner;
